/**
 * LEGACY mock data — kept for reference only.
 * The app uses mockProgressEvents.ts as its real seed data.
 * These exports are intentionally empty to avoid stale type conflicts.
 */

export const mockWorkouts: unknown[] = [];
export const mockFinances: unknown[] = [];
export const mockChallenges: unknown[] = [];
export const mockUserScores: unknown[] = [];
export const mockWeeklyData: unknown[] = [];
